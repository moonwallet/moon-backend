version: '3.7'

services:
  app:
    container_name: app
    image: app
    env_file:
      - .env
    build:
      context: .
      dockerfile: Dockerfile.prod
    command:
      - entrypoints/gunicorn.sh

networks:
  default:
    external: true
    name: dappsheriff
